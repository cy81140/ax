/**
 * Empty stub for lottie-react-native on web platform
 * This prevents web bundling errors while allowing the native code to import lottie-react-native
 */
const LottieView = () => null;

// Export a dummy component that does nothing on web
export default LottieView;

// Export empty mock implementations of all Lottie methods
export const AnimatedLottieView = () => null;
export const LottieViewManager = {};
export const LottieAnimationView = () => null; 